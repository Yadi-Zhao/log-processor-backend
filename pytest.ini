[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = 
    -v
    --tb=short
    --strict-markers

testpaths = tests

pythonpath = lambda/ingestion lambda/worker

[coverage:run]
source = .
omit = 
    tests/*
    venv/*